import React from "react";
import {MenuSection, TitlePlace, H2, Underline} from "./Title.elements.js";

const Title = () => {
	return(
		<MenuSection>
			<TitlePlace>
				<H2> Dani´s Menu </H2>
				<Underline/>
			</TitlePlace>
		</MenuSection>
	)
}

export default Title;